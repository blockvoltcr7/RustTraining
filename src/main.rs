use std::mem;

fn main() {

   //arithmetic

    let mut a = 2+3*4;

    println!("{}", a);

    a += 1;
    println!("incremented = {}", a);

    //rust does not support -- decrement ++ increment
    // you must say a+1 or a-1 or a -= 1
    //-= += *= /= &=




}
